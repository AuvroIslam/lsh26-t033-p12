# Environment variables

# This application has no secrets and needs no environment variables.
# Receipt reading runs entirely in the browser (Tesseract.js) and the ledger is
# stored in localStorage, so there is no API key, token or database URL to set.
