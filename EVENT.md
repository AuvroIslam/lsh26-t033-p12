# Event Start Record

- **Team ID:** `LSH26-T033`
- **Problem ID:** `P12`
- **Repository:** `lsh26-t033-p12`
- **Event start code:** `LSH26-8490-C900`
- **Repository created before release:** No

## Material present before 6:00 PM

The repository did not exist before 6:00 PM. Every commit in its history was made during the event
window, beginning at 19:14, and the first of them added this file. Nothing was carried in from a
previous project.

| Material | Source or original location | What was already present |
|---|---|---|
| None | N/A | Repository was empty |

## Material generated during the event window that was not written line by line

Declared for completeness, since these were produced by tooling rather than typed out:

| Material | Source | When |
|---|---|---|
| Submission kit templates (`README.md`, `EVENT.md`, `LICENSES.md`, `evaluation-manifest.json` skeletons) | LofiStack Hackathon 2026 participant pack | Committed 19:14–19:16, during the event window |
| `P12_personal_ledger_public.json` | LofiStack Hackathon 2026 participant pack | Committed unmodified in `sample-data/`, and copied unmodified to `public/` so the application can load it |
| `create-vite` `react-ts` scaffold (`index.html`, `tsconfig*.json`, `vite.config.ts` and the initial `package.json`) | `npm create vite@latest --template react-ts`, version 9.2.0 | Generated and committed at 19:46, during the event window |

Every file under `src/` — the application, the domain services and the tests — was written during
the event window for this problem. Third-party libraries are listed in `LICENSES.md`.

## Prior experience declaration

A member of the team has previously built an unrelated personal project, an expense-splitting mobile
application. No code, schema, component or asset from it is present in this repository; it is
declared only because familiarity with that problem area informed how this solution was structured.
Everything here was written fresh for P12 during the event window, and the commit history shows it
being built.

## AI assistance

Claude (Anthropic), via Claude Code, was used for scaffolding, implementation and review during the
event window. This is disclosed in full in `README.md`, `LICENSES.md` and
`evaluation-manifest.json`. The work was read, tested and accepted by the team.

## Declaration

This file was added in the first event-work commit. The team will preserve the repository history
until results are announced.
